# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build matches-scraper`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t matches-scraper`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system matches-scraper && \
          adduser --system -G matches-scraper matches-scraper

COPY dist/apps/matches-scraper matches-scraper/
RUN chown -R matches-scraper:matches-scraper .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix matches-scraper --omit=dev -f install

CMD [ "node", "matches-scraper" ]
